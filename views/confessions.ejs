<%- include('partials/header') %>
<div class="align"> 
   <h1>These are the recent confessions....</h1>
</div>

<% for(let i=users.length-1;i>=0;i--){ %>
   <% if(users[i].secrets){ %>
   <div>
      <p><%=users[i].Date%></p>
      <p class="mainsec"><%= users[i].secrets %></p>
      <br><br>
   </div>
<% }} %>


<%- include('partials/footer') %>
